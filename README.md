# CFB 27 Defensive Playbook Lab — v4.28 Mapping Checkpoint

This package is an internal checkpoint and is not the final completed defensive database.

## Files

- `index.html` — standalone v4.28 application
- `VERIFIED_PLAY_ART.md` — current verified mapping count by formation
- `example-backup.json` — example browser backup file

## Recommended GitHub use

Upload these files to a separate branch named:

`mapping-checkpoint-v4.28`

Do not merge that branch into `main` yet. Keeping it separate protects the completed mapping work without changing the currently published GitHub Pages application.
